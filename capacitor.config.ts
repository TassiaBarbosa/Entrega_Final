import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.nerdsphera',
  appName: 'NerdSphera',
  webDir: 'www'
};

export default config;
